@model IEnumerable<NVN123.Models.NVN>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NVNName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Genre)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NVNName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Genre)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.NVNID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.NVNID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.NVNID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
